[[ -S "$HOME/.profile" ]] && source "$HOME/.profile" # Load the default .profile

# MYSQL path
export PATH=/usr/local/mysql/bin:$PATH

##
# Your previous /Users/jtrope/.bash_profile file was backed up as /Users/jtrope/.bash_profile.macports-saved_2014-02-24_at_12:40:20
##

### Folder Shortcuts
export CODE_PATH=/var/www/offerpop #offerpop root repo
export APP_PATH=$CODE_PATH/v2/dashboard_project
alias cdpop="cd $CODE_PATH"
alias cdapp="cd $APP_PATH; setvm"

### Restart Apache server shortcut
alias reapache="sudo apachectl restart"

# Displays Git Branch in Command Prompt
function parse_git_branch() {
  git branch --no-color 2> /dev/null | sed -e '/^[^*]/d' -e "s/* \(.*\)/\1/"
}

# Prompt 1: "username@hostname:"
PS1="\[$(tput setaf 1)\]\e[\e[34;40m\u\e[m@\e[31;40m\h\e[m:\]"

# Prompt 2: "path/to/where/you/are"
PS1="$PS1\e[32;40m\w\e[m"

# Prompt 3: "(gitbranch)"
PS1="$PS1 \$([[ -n \$(git branch 2> /dev/null) ]] && echo \" \")\[\033[1;33m\]\$(parse_git_branch)\[\033[1;37m\]\n\$ \[$(tput sgr0)\]"

# PS1="$txtylw\w$txtgrn\$(branch)$txtwht ∴$txtrst "
##

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

# Highligh directories
export CLICOLOR=1
export LSCOLORS=ExFxCxDxBxegedabagacad

MYSQL_PASSWORD="sharf84mys"
export MYSQL_PASSWORD
##
# Your previous /Users/jtrope/.bash_profile file was backed up as /Users/jtrope/.bash_profile.macports-saved_2014-03-31_at_14:49:07
##

# MacPorts Installer addition on 2014-03-31_at_14:49:07: adding an appropriate PATH variable for use with MacPorts.
export PATH=/opt/local/bin:/opt/local/sbin:$PATH
# Finished adapting your PATH environment variable for use with MacPorts.

# Set Offerpop VM
alias setvm="source ~/vm/offerpop-site-env/bin/activate"

export PYTHONPATH=/var/www/offerpop/Engine:/var/www/offerpop/Lib:/var/www/offerpop:/var/www/offerpop/v2

export DYLD_LIBRARY_PATH=/usr/local/mysql/lib:$DYLD_LIBRARY_PATH
